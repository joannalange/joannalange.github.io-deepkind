---
import content from '@data/content';
const { description, legal, connect, copyright, location } = content.footer;
---

<footer class="footer">
  <div class="wrap">

    <div class="footer__top">

      <!-- Brand -->
      <div>
        <img src="/assets/logo.svg" alt="DeepKind" class="footer__logo-img" width="140" height="28" />
        <p class="footer__tagline">{description}</p>
      </div>

      <!-- Legal -->
      <div>
        <p class="footer__col-heading">Prawne</p>
        <ul class="footer__links">
          {legal.map(item => (
            <li><a href={item.url} class="footer__link">{item.name}</a></li>
          ))}
        </ul>
      </div>

      <!-- Connect -->
      <div>
        <p class="footer__col-heading">Connect</p>
        <ul class="footer__links">
          {connect.map(item => (
            <li>
              <a
                href={item.url}
                class="footer__link"
                target={item.url.startsWith('http') ? '_blank' : undefined}
                rel={item.url.startsWith('http') ? 'noopener noreferrer' : undefined}
              >{item.name}</a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Location -->
      <div>
        <p class="footer__col-heading">Location</p>
        <p class="footer__location">{location}</p>
      </div>

    </div>

    <div class="footer__rainbow" aria-hidden="true"></div>

    <div class="footer__bottom">
      <p class="footer__copy">{copyright}</p>
    </div>

  </div>
</footer>
